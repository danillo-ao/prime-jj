import { styled } from '@theme/theme';

export const AboutBenefitsCardsWrapper = styled('div', {
  width: '100%',
  position: 'relative',
  display: 'flex',
  flexDirection: 'row',
  flexWrap: 'wrap',
});
